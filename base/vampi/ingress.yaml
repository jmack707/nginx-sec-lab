---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vampi
  namespace: vampi
  annotations:
    nginx.org/proxy-connect-timeout: "30s"
    nginx.org/proxy-read-timeout: "30s"
    nginx.org/client-max-body-size: "1m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - vampi.lab.local
      secretName: vampi-tls
  rules:
    - host: vampi.lab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vampi
                port:
                  number: 80
