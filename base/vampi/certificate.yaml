---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vampi-tls
  namespace: vampi
spec:
  secretName: vampi-tls
  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - vampi.1broken.net
