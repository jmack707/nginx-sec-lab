# cni/cilium/values-base.yaml
# Cilium on k3d — baseline config, no BIG-IP VTEP
# Use with: task cni:install CNI=cilium
# CIS pool-member-type: nodeport (BIG-IP cannot reach pod IPs without VTEP)

# ── Tunnel mode ────────────────────────────────────────────────────────────
# VXLAN is required for kind (k3d nodes are Docker containers — no native routing)
tunnel: vxlan

# ── kube-proxy compatibility ───────────────────────────────────────────────
# k3s ships with kube-proxy — do NOT replace it in base mode
# Set to "partial" to enable NodePort/ExternalIP handling by Cilium while
# keeping kube-proxy for ClusterIP. Full replacement breaks kind networking.
kubeProxyReplacement: "disabled"

# ── IPAM ──────────────────────────────────────────────────────────────────
ipam:
  mode: "kubernetes"              # Respect podCIDR assigned by k3d per node
  operator:
    clusterPoolIPv4PodCIDRList:
      - "10.244.0.0/16"

# ── k3d-specific settings ─────────────────────────────────────────────────
# Disable features that require hardware/bare-metal capabilities
hostServices:
  enabled: false
externalIPs:
  enabled: true
nodePort:
  enabled: true

# ── Image ──────────────────────────────────────────────────────────────────
image:
  pullPolicy: IfNotPresent        # Avoid repeat pulls in k3d

# ── Observability ──────────────────────────────────────────────────────────
prometheus:
  enabled: true
  port: 9962

operator:
  prometheus:
    enabled: true

hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true                 # Hubble UI — port-forward for visibility

# ── Resource limits (lab sizing) ──────────────────────────────────────────
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
